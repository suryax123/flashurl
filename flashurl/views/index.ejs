<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlashURL - Shorten URLs & Earn Money | Best URL Shortener</title>
    <meta name="description" content="Shorten your URLs and earn money with every click. Industry-leading rates, instant payouts, and user-friendly dashboard.">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-brand">⚡ FlashURL</a>
            <button class="nav-toggle" id="navToggle">
                <i class="fas fa-bars"></i>
            </button>
            <div class="nav-links" id="navLinks">
                <a href="/publisher-rates" class="nav-link">💰 Publisher Rates</a>
                <a href="/about" class="nav-link">ℹ️ About Us</a>
                <% if (user) { %>
                    <a href="/dashboard" class="nav-link">
                        <i class="fas fa-chart-line"></i> Dashboard
                    </a>
                    <div class="nav-dropdown">
                        <button class="dropdown-toggle" id="userDropdown">
                            <% if (user.avatar) { %>
                                <img src="<%= user.avatar %>" alt="Avatar" class="nav-avatar">
                            <% } else { %>
                                <div class="nav-avatar" style="background: linear-gradient(135deg, #6c5ce7, #00cec9); width: 30px; height: 30px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; color: white; font-weight: bold; margin-right: 8px;"><%= user.name.charAt(0).toUpperCase() %></div>
                            <% } %>
                            <span><%= user.name %></span>
                            <i class="fas fa-chevron-down"></i>
                        </button>
                        <div class="dropdown-menu">
                            <a href="/dashboard" class="dropdown-item">
                                <i class="fas fa-chart-line"></i> Dashboard
                            </a>
                            <a href="/dashboard/settings" class="dropdown-item">
                                <i class="fas fa-cog"></i> Settings
                            </a>
                            <hr>
                            <a href="/auth/logout" class="dropdown-item">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <a href="/auth/login" class="nav-link">
                        <i class="fas fa-sign-in-alt"></i> Login
                    </a>
                    <a href="/auth/register" class="btn btn-primary" style="padding: 10px 20px;">
                        <i class="fas fa-user-plus"></i> Sign Up
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container">
        <!-- Hero Section -->
        <header class="hero-header">
            <h1>⚡ Shorten URLs & Earn Money</h1>
            <p class="tagline">The most user-friendly URL shortener with competitive rates</p>
            <p class="hero-subtitle">Join thousands of publishers earning money from their links</p>
        </header>

        <main>
            <!-- URL Shortener Card -->
            <div class="card shortener-card">
                <h2 style="text-align: center; margin-bottom: 25px; color: #2d3436;">
                    <i class="fas fa-link"></i> Create Your Short Link
                </h2>
                <form id="urlForm">
                    <div class="input-group">
                        <input 
                            type="url" 
                            id="originalUrl" 
                            placeholder="Paste your long URL here..." 
                            required
                        >
                        <button type="submit" id="submitBtn" class="btn btn-primary">
                            <i class="fas fa-bolt"></i> Shorten
                        </button>
                    </div>
                </form>

                <div id="result" class="result hidden">
                    <p style="color: #00b894; font-weight: 600;">
                        <i class="fas fa-check-circle"></i> Your shortened URL:
                    </p>
                    <div class="result-box">
                        <input type="text" id="shortUrl" readonly>
                        <button type="button" id="copyBtn" class="btn btn-success">
                            <i class="fas fa-copy"></i> Copy
                        </button>
                    </div>
                    <p id="copyMsg" class="copy-msg hidden">
                        <i class="fas fa-check"></i> Copied to clipboard!
                    </p>
                    <% if (!user) { %>
                        <div class="signup-prompt">
                            <p><i class="fas fa-info-circle"></i> <strong>Want to track this link and earn money?</strong></p>
                            <a href="/auth/register" class="btn btn-primary">
                                <i class="fas fa-user-plus"></i> Create Free Account
                            </a>
                        </div>
                    <% } %>
                </div>

                <div id="error" class="error hidden"></div>
            </div>

            <!-- Why Choose FlashURL -->
            <div class="card comparison-section">
                <h2 style="text-align: center; margin-bottom: 30px;">
                    <i class="fas fa-star"></i> Why Choose FlashURL?
                </h2>
                <div class="comparison-grid">
                    <div class="comparison-item">
                        <div class="comparison-icon" style="background: linear-gradient(135deg, #74b9ff, #0984e3);">
                            <i class="fas fa-user-friends"></i>
                        </div>
                        <h3>User-Friendly Interface</h3>
                        <p>Clean, intuitive dashboard that anyone can use. No technical knowledge required. Create and manage links in seconds.</p>
                    </div>
                    <div class="comparison-item">
                        <div class="comparison-icon" style="background: linear-gradient(135deg, #55efc4, #00b894);">
                            <i class="fas fa-dollar-sign"></i>
                        </div>
                        <h3>Highest Rates</h3>
                        <p>Industry-leading CPM rates up to $12 for Tier 1 countries. Earn more than other shorteners with the same traffic.</p>
                    </div>
                    <div class="comparison-item">
                        <div class="comparison-icon" style="background: linear-gradient(135deg, #a29bfe, #6c5ce7);">
                            <i class="fas fa-chart-line"></i>
                        </div>
                        <h3>Detailed Analytics</h3>
                        <p>Real-time statistics with country breakdown, device tracking, and earnings reports. Make data-driven decisions.</p>
                    </div>
                    <div class="comparison-item">
                        <div class="comparison-icon" style="background: linear-gradient(135deg, #ffeaa7, #fdcb6e);">
                            <i class="fas fa-clock"></i>
                        </div>
                        <h3>Quick Redirects</h3>
                        <p>Fast 3-gate system with minimal wait times. Better user experience = higher click-through rates for you.</p>
                    </div>
                    <div class="comparison-item">
                        <div class="comparison-icon" style="background: linear-gradient(135deg, #fd79a8, #e84393);">
                            <i class="fas fa-wallet"></i>
                        </div>
                        <h3>Low Minimum Payout</h3>
                        <p>Withdraw at just $5 minimum. Multiple payment options including UPI, Bank Transfer, and PayPal.</p>
                    </div>
                    <div class="comparison-item">
                        <div class="comparison-icon" style="background: linear-gradient(135deg, #fab1a0, #e17055);">
                            <i class="fas fa-users"></i>
                        </div>
                        <h3>Referral Program</h3>
                        <p>Earn 20% commission from referred users' earnings forever. Build passive income with our generous affiliate system.</p>
                    </div>
                </div>
            </div>

            <!-- Features Grid -->
            <div class="features">
                <div class="feature">
                    <span class="icon">🚀</span>
                    <h3>Lightning Fast</h3>
                    <p>Instant URL shortening with optimized infrastructure</p>
                </div>
                <div class="feature">
                    <span class="icon">🔒</span>
                    <h3>100% Secure</h3>
                    <p>SSL encryption and spam protection for all links</p>
                </div>
                <div class="feature">
                    <span class="icon">📊</span>
                    <h3>Free Forever</h3>
                    <p>No hidden fees, no premium plans required</p>
                </div>
            </div>

           
            <!-- CTA Section -->
            <% if (!user) { %>
            <div class="cta-section">
                <h2><i class="fas fa-rocket"></i> Ready to Start Earning?</h2>
                <p>Create your free account and start monetizing your links today!</p>
                <div class="cta-buttons">
                    <a href="/auth/register" class="btn btn-primary btn-lg">
                        <i class="fas fa-user-plus"></i> Sign Up Free
                    </a>
                    <a href="/publisher-rates" class="btn btn-secondary btn-lg">
                        <i class="fas fa-dollar-sign"></i> View Rates
                    </a>
                </div>
            </div>
            <% } %>
        </main>

        <footer>
            <div class="footer-links">
                <a href="/publisher-rates">Publisher Rates</a>
                <span>•</span>
                <a href="/about">About Us</a>
                <span>•</span>
                <a href="/auth/register">Sign Up</a>
            </div>
            <p>&copy; 2025 FlashURL. Made with ❤️ | All rights reserved.</p>
        </footer>
    </div>

    <script src="/js/main.js"></script>
    <script>
        // Mobile nav toggle
        document.getElementById('navToggle')?.addEventListener('click', function() {
            document.getElementById('navLinks').classList.toggle('active');
        });

        // User dropdown toggle
        document.getElementById('userDropdown')?.addEventListener('click', function() {
            this.parentElement.classList.toggle('active');
        });

        // Close dropdown when clicking outside
        document.addEventListener('click', function(event) {
            const dropdown = document.querySelector('.nav-dropdown');
            if (dropdown && !dropdown.contains(event.target)) {
                dropdown.classList.remove('active');
            }
        });
    </script>
</body>
</html>
